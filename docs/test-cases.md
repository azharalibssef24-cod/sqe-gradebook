# GradeBook Test Cases

| ID | Title | Requirement | Preconditions | Steps | Expected | Priority | Type |
|---|---|---|---|---|---|---|---|
| TC-01 | Add Valid Score | REQ-01 — Valid Score | A Student object exists with a unique student ID. | 1. Create `Student("Ali", "101")`.<br>2. Call `add_score(80)`.<br>3. Check `student.scores`. | Score `80` is added successfully to the scores list. | High | Functional / Positive |
| TC-02 | Reject Negative Score | REQ-02 — Negative Score Rejection | A Student object exists with a unique student ID. | 1. Create `Student("Ali", "102")`.<br>2. Call `add_score(-5)`. | A `ValueError` with message `"Score cannot be negative"` is raised and the score is not added. | High | Functional / Negative |
| TC-03 | Reject Non-Numeric Score | REQ-03 — Non-Numeric Score Rejection | A Student object exists with a unique student ID. | 1. Create `Student("Ali", "103")`.<br>2. Call `add_score("abc")`. | The invalid input is rejected with an error and is not added to the scores list. | High | Functional / Negative |
| TC-04 | Accept Minimum Score | REQ-07 — Score Boundaries | A Student object exists with a unique student ID. | 1. Create `Student("Ali", "104")`.<br>2. Call `add_score(0)`.<br>3. Check `student.scores`. | Score `0` is accepted and added successfully. | High | Boundary / Positive |
| TC-05 | Accept Maximum Score | REQ-07 — Score Boundaries | A Student object exists with a unique student ID. | 1. Create `Student("Ali", "105")`.<br>2. Call `add_score(100)`.<br>3. Check `student.scores`. | Score `100` is accepted and added successfully. | High | Boundary / Positive |
| TC-06 | Reject Score Above 100 | REQ-07 — Score Boundaries | A Student object exists with a unique student ID. | 1. Create `Student("Ali", "106")`.<br>2. Call `add_score(101)`. | A `ValueError` with message `"Score cannot be greater than 100"` is raised and the score is not added. | High | Boundary / Negative |
| TC-07 | Calculate Average | REQ-05 — Average Calculation | A Student object exists with a unique student ID. | 1. Create `Student("Ali", "107")`.<br>2. Add scores `80`, `70`, and `90`.<br>3. Call `average()`. | The method returns `80.0`. | High | Functional / Positive |
| TC-08 | Empty Score Average | REQ-06 — Empty Score Average | A Student object exists with a unique student ID and no scores. | 1. Create `Student("Ali", "108")`.<br>2. Call `average()`. | The method returns `0.0`. | High | Functional / Boundary |
| TC-09 | Reject Duplicate Student ID | REQ-04 — Duplicate Student ID | A Student with ID `"109"` already exists. | 1. Create `Student("Ali", "109")`.<br>2. Attempt to create `Student("Ahmed", "109")`. | A `ValueError` with message `"Student ID already exists"` is raised and the duplicate student is not created. | High | Functional / Negative |
| TC-10 | Add Multiple Valid Scores | REQ-01 — Valid Score | A Student object exists with a unique student ID. | 1. Create `Student("Ali", "110")`.<br>2. Add scores `60`, `80`, and `90`.<br>3. Check `student.scores`. | All three valid scores are added successfully. | Medium | Functional / Positive |
| TC-11 | Case-Insensitive Student Name Comparison | REQ-08 — Student Name Comparison | Two Student objects exist with unique student IDs. | 1. Create `Student("Ali", "111")`.<br>2. Create `Student("ALI", "113")`.<br>3. Attempt to compare the two student names using the required name-comparison functionality. | The system considers `"Ali"` and `"ALI"` equal because the comparison is case-insensitive. | High | Functional / Positive |
| TC-12 | Reject Score Below Minimum | REQ-02 — Negative Score Rejection | A Student object exists with a unique student ID. | 1. Create `Student("Ali", "112")`.<br>2. Call `add_score(-1)`. | A `ValueError` with message `"Score cannot be negative"` is raised and the score is not added. | High | Boundary / Negative |